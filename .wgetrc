# Timeout and retries
timeout = 60
tries = 3
retry_connrefused = on

# Timestamps and directory behavior
timestamping = on
no_parent = on

# Use last component of redirect URL for filename
trust_server_names = on

# Add proper extensions to downloaded files
adjust_extension = on
