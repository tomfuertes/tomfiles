# Dotfiles Manifest - single source of truth for sync
# Lines: paths to sync | !prefix = exclude | # comments

# === ROOT DOTFILES ===
.zshrc
.gitconfig
.gitignore
.curlrc
.wgetrc
.editorconfig
.hushlogin

# === GHOSTTY ===
.config/ghostty/

# === CLAUDE CONFIG ===
.claude/settings.json
.claude/CLAUDE.md
.claude/keybindings.json
.claude/hooks/
.claude/rules/
.claude/skills/

# === NEVER SYNC (sensitive/local-only) ===
!.claude/settings.local.json
!.DS_Store

# npx-managed skills are auto-excluded via MANAGED_SKILLS in bootstrap.sh
